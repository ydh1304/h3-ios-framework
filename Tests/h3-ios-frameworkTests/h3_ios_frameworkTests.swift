import XCTest
@testable import h3_ios_framework

final class h3_ios_frameworkTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(h3_ios_framework().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
